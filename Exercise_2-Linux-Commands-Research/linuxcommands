- `cat`: Concatenate and display the content of files.

- `cal`: Display a calendar.

- `bc`: A calculator with arbitrary precision.

- `help`: Display help information for shell builtins.

- `ping`: Send ICMP ECHO_REQUEST to network hosts to check connectivity.

- `gzip`: Compress or decompress files using the gzip algorithm.

- `whatis`: Display a one-line description of a command.

- `free`: Display amount of free and used memory in the system.

- `banner`: Print characters in a large banner format.

- `history`: Display the command history list for the current user.

